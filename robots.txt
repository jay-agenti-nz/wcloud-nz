# robots.txt — White Cloud (wcloud.nz)
#
# Everything here is open, deliberately. Being readable by AI assistants
# is part of what this business sells, so the permissions are stated
# out loud rather than left unsaid — silence is "neither granted nor
# restricted", which the more cautious crawlers read as a no.
#
# One group, on purpose. A crawler obeys the most specific User-agent
# group that matches it and ignores every other group, so naming bots
# individually would mean each of them missing the Content-Signal line
# below. Anything named here inherits nothing from "*".

User-agent: *
Content-Signal: search=yes,ai-input=yes,ai-train=yes
Allow: /

# Cloudflare's own endpoints — infrastructure, not content.
Disallow: /cdn-cgi/

Sitemap: https://wcloud.nz/sitemap.xml
